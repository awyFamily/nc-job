feign:
  hystrix:
    enabled: true
  okhttp:
    enabled: true
  httpclient:
    enabled: false
  client:
    config:
      default:
        connectTimeout: 10000
        readTimeout: 10000
  compression:
    request:
      enabled: true
    response:
      enabled: true
nc:
 job:
  provider:
    jobServerId: nc-job-example
    taskPackagePath: com.yhw.nc.job.example.handler
